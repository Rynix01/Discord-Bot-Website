<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="shortcut icon" href="<%= links.icon %>" type="image/x-icon" />
    <link href="/css/output.css" rel="stylesheet" />
  </head>
  <body
    class="bg-gradient-to-br from-gray-900 to-gray-800 min-h-screen text-white"
  >
    <%- include('partials/header') %>

    <div class="container mx-auto px-4 py-20">
      <div
        class="max-w-4xl mx-auto bg-gray-900 bg-opacity-75 rounded-lg shadow-lg overflow-hidden"
      >
        <div class="flex flex-col md:flex-row">
          <div class="md:w-2/3 p-8">
            <h1 class="text-4xl font-bold text-white mb-4"><%= title %></h1>
            <p class="text-gray-300 leading-relaxed mb-6"><%= briefing %></p>
            <a
              href="/commands"
              class="inline-block px-6 py-3 mt-4 bg-indigo-600 hover:bg-indigo-500 text-white font-semibold rounded-lg transition duration-300"
            >
              Explore Commands
            </a>
            <a
              href="<%= links.invite %>"
              target="_blank"
              class="inline-block mt-2 px-8 py-3 bg-green-600 hover:bg-green-500 text-white font-semibold rounded-lg transition duration-300"
            >
              Add to Discord
            </a>
          </div>

          <div
            class="md:w-1/3 bg-gray-800 flex justify-center items-center p-8"
          >
            <div
              class="bg-white rounded-full p-1 shadow-lg transform transition duration-500 hover:scale-105"
            >
              <img
                src="<%= links.icon %>"
                alt="Bot Logo"
                class="w-48 h-48 object-cover rounded-full"
              />
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="container mx-auto px-4 py-20">
      <h2 class="text-3xl font-bold text-center text-white mb-12">
        Bot Features
      </h2>

      <div class="grid gap-8 lg:grid-cols-3">
        <% features.forEach(feature => { %>
        <div
          class="bg-gray-800 rounded-lg shadow-lg p-6 transition-transform transform hover:scale-105"
        >
          <h3 class="text-2xl font-bold text-indigo-500 mb-4">
            <%= feature.title %>
          </h3>
          <p class="text-gray-300"><%= feature.description %></p>
        </div>
        <% }); %>
      </div>
    </div>

    <div class="container mx-auto px-4 py-20">
      <div class="bg-gray-800 rounded-lg shadow-lg p-6">
        <h2 class="text-3xl font-bold text-center text-white mb-6">
          Add the Bot to Your Server
        </h2>
        <p class="text-gray-300 text-center mb-6">
          To add the bot to your Discord server, simply click the button below.
          Make sure you have the necessary permissions to add bots to your
          server.
        </p>
        <div class="flex justify-center">
          <a
            href="<%= links.invite %>"
            target="_blank"
            class="inline-block px-8 py-3 bg-green-600 hover:bg-green-500 text-white font-semibold rounded-lg transition duration-300"
          >
            Add to Discord
          </a>
        </div>
      </div>
    </div>

    <%- include('partials/footer') %>
  </body>
</html>
